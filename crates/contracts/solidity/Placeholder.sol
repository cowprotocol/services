// SPDX-License-Identifier: MIT
pragma solidity ^0.8;

/// @dev A placeholder contract for the new support contract scaffolding.
contract Placeholder {
    function addOne(uint256 value) external pure returns (uint256) {
        return value + 1;
    }
}
